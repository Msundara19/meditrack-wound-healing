# MediTrack – Wound Healing Monitoring

This is a packaged demo repo for an AI-assisted wound monitoring system.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
pip install -r requirements.txt
streamlit run src/meditrack/dashboard/app.py
```
